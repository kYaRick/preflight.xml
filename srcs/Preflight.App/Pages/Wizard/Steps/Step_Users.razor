@inject IStringLocalizer<SharedResources> L

@*
    Users wizard step. UsersSection owns its own header + accounts table +
    first-logon/expiration/lockout sub-forms. No extra wrapper title - that
    duplicated the section's own "Users" heading.
*@

<FluentCard Style="padding: 24px;">
    <UsersSection />
</FluentCard>
